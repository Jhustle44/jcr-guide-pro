import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jcrguidepro.app',
  appName: 'JCR Guide Pro',
  webDir: 'client/dist',
  bundledWebRuntime: false
};

export default config;
